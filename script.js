// footer  1
var mbappe = document.querySelector("footer");
// footer  2
for(let count = 1; 0 <= count; count++) {
    mbappe.addEventListener('click',function(){
    
        console.log("click" + "numéro" + count);
    });
}